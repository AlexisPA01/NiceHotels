import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'emhotels-frontend-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
